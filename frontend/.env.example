# Sanity.io project configuration
VITE_SANITY_PROJECT_ID=your_project_id
VITE_SANITY_DATASET=dev
# API token with Editor role — required for creating/updating documents
# Generate at https://sanity.io/manage → project → API → Tokens
VITE_SANITY_TOKEN=your_sanity_token
